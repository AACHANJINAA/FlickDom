# FlickDom (플릭덤)

> **Freedom 기반 물리 점령 게임**
> 알까기의 정교한 물리적 조작감과 보드판 위의 치밀한 공간 점령 전략이 결합된 대전형 보드게임

## 📖 게임 소개
**FlickDom**은 5x5 격자 공간 위에서 오브젝트를 물리적으로 튕겨(Flicking) 자신의 지배력을 구축하고, 난이도별 점령 카드를 선점하는 고도의 심리 전략 게임입니다.

### 🎯 핵심 룰
- **보드 & 맵**: 5x5 물리 플레이 보드와 5x5 논리 토큰 맵으로 구성
- **토큰 제한**: 각 플레이어당 최대 5개의 지역 지배 토큰 배치 가능 (초과 시 기존 토큰 재배치)
- **승리 조건**: EASY(2칸) → NORMAL(3칸) → HARD(4칸) 패턴의 지역 점령 카드를 점령하여, 가장 많은 카드를 획득한 플레이어가 최종 승리
- **물리 예외 처리**: 튕겨진 토큰은 반드시 외곽 벽이나 다른 토큰에 충돌해야 하며, 아무것도 부딪히지 않거나 장외로 밀려나면 무효 처리됨

## 💻 시스템 구조 및 코어 로직
게임의 코어 로직은 유한 상태 머신(FSM)을 기반으로 물리 연산(비동기)과 턴제 논리를 완벽하게 동기화합니다.
(현재 `Scripts` 폴더에 박신우님이 담당한 핵심 아키텍처 코드가 구현되어 있습니다.)

### 코어 게임 흐름 (`GameModeManager.cs`, `TokenMapManager.cs`)
1. **Ready State**: 라운드 초기화 (선후공 판정) 및 2D 분할 화면 대기
2. **Flicking State**: 3D 45도 쿼터뷰(Isometric)로 전환하여 알까기 조작 활성화
3. **Physics Processing**: 실시간 물리 연산 진행 (모든 토큰의 속도가 0이 될 때까지 대기)
4. **Placement State**: 2D 화면 복귀. 정지된 토큰 좌표를 5x5 논리 그리드로 매핑(`GridCellCandidateResolver`), 소유권 강제 탈취 및 재배치 룰 수행
5. **Card Match State**: 카드 패턴 데이터와 보드 데이터를 비교하여 카드 획득 처리

## 👥 팀원 및 역할 분담 (R&R)
- **임찬진 (Client A)**: 물리 엔진 코어 연산, 2D/3D 카메라 블렌딩 트랜지션(Cinemachine), 입력 및 궤적 시각화, 물리-그리드 좌표 판정 알고리즘
- **박신우 (Client B)**: 게임 아키텍처 및 FSM 개발, 2D 분할 화면 UI 구현, 토큰 맵 데이터(2차원 배열) 관리 및 스와프 규칙 구현, 카드 패턴 매칭 비트 연산 구현
- **황인성 (Model / VFX)**: 3D 보드/토큰 모델링, 카드 UI 아트워크 디자인, 드래그 인디케이터 및 충돌 파티클 등 인게임 VFX 연출

---

## 🤖 AI Agent Instructions (작업 지침)
이 프로젝트를 보조하는 AI 에이전트는 다음 지침을 엄격히 준수해야 합니다:
1. **기본 플랜 필독:** 모든 작업 전, `.agents/implementation_plan.md` (상세 개발 플랜) 파일을 무조건 읽고 현재 목표를 숙지할 것.
2. **기획 (Game Design):** 기획 관련 작업이나 아이데이션 시에는 반드시 `game-design` 스킬을 장착하고 지침을 참고할 것.
3. **프로그래밍 (Programming):** C# 스크립트 작성 및 유니티 구조 설계 시에는 반드시 `unity-development` 스킬을 장착하고 지침을 참고할 것.
